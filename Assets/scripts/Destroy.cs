using UnityEngine;

public class Destroy : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        // Destroy the game object at the start of the game
        Destroy(gameObject);
    }
}
